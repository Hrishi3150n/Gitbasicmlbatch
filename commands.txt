git config --global user.name "username"

git config --global user.email "user email"

git init => for initializing git in pwd

git status => list alll untracked files

git add -A => adding all changes to staging area

git commit -m "commit message" =>adding stages files to local repository

git remote add luminar git remote add origin https://github.com/Hrishi3150n/Gitbasicmlbatch.git

git remote -v
